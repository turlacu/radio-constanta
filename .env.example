# API Configuration
# Leave empty to use relative path (/api/news) when deployed
# Set to your server URL for local development if needed
# Example: VITE_API_URL=https://rcplay.turlacu.ro/api/news
VITE_API_URL=

# Weather API Configuration
# Get your free API key from https://openweathermap.org/api
# Sign up, verify email, and copy your API key
VITE_WEATHER_API_KEY=

# Server Configuration
NODE_ENV=development
PORT=3001

# CORS Configuration
# Comma-separated list of allowed origins for production
# Example: ALLOWED_ORIGINS=https://rcplay.turlacu.ro,https://www.radioconstanta.ro
ALLOWED_ORIGINS=

# Logging Configuration
# Options: error, warn, info, debug
LOG_LEVEL=debug

# Admin Panel Configuration (Server-side only)
# Admin password hash (bcrypt). Default password is "admin123"
# To generate a new hash, run: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('your_password', 10));"
ADMIN_PASSWORD_HASH=$2b$10$K2wPm5W9ZMUoWo8HPVCnMORGDjANxJNcKUK4v5FMb8q2TvjM5rIom

# JWT Secret for admin authentication
# CRITICAL: Generate a secure random secret for production!
# Generate: openssl rand -base64 64
# Must be at least 32 characters
JWT_SECRET=your-secret-key-change-in-production
